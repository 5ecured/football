import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    center: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    },
    image: {
        marginTop: '15px',
        height: '200px',
        width: '225px'
    }
})
