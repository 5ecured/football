import { makeStyles } from "@mui/styles"

export const useStyles = makeStyles({
    image: {
        height: '275px',
        width: '275px',
    },
    center: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    },
    cardActions: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    },
})