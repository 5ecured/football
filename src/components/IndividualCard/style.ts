import { makeStyles } from "@mui/styles"

export const useStyles = makeStyles({
    image: {
        height: '300px',
        width: '300px',
    },
    center: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
})